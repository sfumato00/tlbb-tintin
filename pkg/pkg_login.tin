#nop {login}

#ACTION {^Please input gb or big5:} {gb;$username;$password;} {1};

#ACTION {^重新连线完毕。$} {
    init_task;
} {1};

#ACTION {^目前权限：} {
    #FORMAT {env[init_login]} {%T};
    cmd {$reposition_path};
    init_task;
} {1};