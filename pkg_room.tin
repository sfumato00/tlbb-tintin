#NOP Room package

#ACTION {^%u - $} {
    #var env[room] {%1};
    #NOP #ECHO {debug: 进入房间 $env[room]};
} {5};

#ACTION {这里%u的出口是%*。$} {
    #ECHO {Exits: %2};
    #VARIABLE {env[exits]} {%2};
    #FORMAT {env[room_updated]} {%T};
}
